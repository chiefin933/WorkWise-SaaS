"""The reports module generates all data dynamically by querying other app models (employees, payroll, attendance, leave). No database models are required here."""

from django.db import models

# Create your models here.
